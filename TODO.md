# HELP
- [ ] Retrain the model with another compute_metrics, since it does not compare the embeddings to a score that does not contain the same embeddings. So how do we Validation
- [ ] Create BLUERT score to use on the test set
- [ ] Create Style Fidelity Score, where we apodtate using the BERTscore and adjust it to the (completion -> GenZ_Completion) and the (GenZ_Completion vs prediction) 
- [ ] Try it with more EPOCHS so like 10 
- [ ] Add Early stop to trainer
- [ ] Display our data and our new data analysis on the generated data for the TL;DR
- [ ] Try the other prefix for the training. 
- [ ] IMPORTANT for the validation 